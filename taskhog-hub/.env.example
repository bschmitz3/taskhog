# Device auth (Bearer token — same value provisioned on the ESP32)
TASKHOG01_TOKEN=change-me-to-a-long-random-string

# Todoist REST API token
TODOIST_TOKEN=

# LLM (OpenAI-compatible)
LLM_MODEL=gpt-4o-mini
LLM_ENDPOINT=https://api.openai.com/v1
LLM_API_KEY=
